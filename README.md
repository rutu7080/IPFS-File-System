# Sample IPFS Application
Demo storage system using IPFS 

Start Daemon node(Keep Terminal On)

```shell
ipfs Daemon
```

In another Terminal Start Server(Keep Terminal On)
```shell
cd ipfs-ui
node server.js
```
In another Terminal Start App
```shell
cd frontend
npm start
```